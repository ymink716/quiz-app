server {
    listen 3000;  // nginx 서버가 listen하고 있는 포트

    location / {
        
        root /usr/share/nginx/html;  // HTML 파일이 위치할 루트 설정

        index index.html index.htm;  // 사이트의 index 페이지 파일명 설정

        try_files $uri/ /index.html;  // React Router를 사용하여 페이지간 이동할 때 필요

    }
}