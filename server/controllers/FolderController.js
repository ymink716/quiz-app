const { Folder } = require('../models/folder');


exports.createFolder = async (req, res, next) => {
    try {
        
    } catch (error) {
        console.error(error);
        next(error);
    }
}